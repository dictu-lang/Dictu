/**
 * end.du
 *
 * Testing the Env functions:
 *    - get(), set()
 *
 */

assert(Env.get("bad key") == nil);
Env.set("test", "test");
assert(Env.get("test") == "test");
Env.set("test", nil);
assert(Env.get("test") == nil);