/**
 * chdir.du
 *
 * Testing the System.chdir() function
 *
 * chdir() changes the current working directory to the given path.
 */
import System;

var cwd = System.getCWD().unwrap();
assert(cwd != "");

System.chdir("/tmp");
var cwd2 = System.getCWD().unwrap();

assert(cwd2 != "");
assert(cwd != cwd2);
