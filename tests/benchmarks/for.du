var list = [];

var start = clock();

for (var i = 0; i < 1000000; ++i) {
    list.push(i);
}

var sum = 0;
var leng = len(list);

for (var i = 0; i < leng; ++i) {
    sum += i;
}

print(sum);
print("Elapsed: {}".format(clock() - start));