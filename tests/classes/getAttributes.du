/**
 * getAttributes.du
 *
 * Testing the obj.getAttributes() method
 *
 */

class Test {
    var x = 10;

    init(private priv = 10, var pub = 20) {
        this.prop = 30;
    }

    private privMethod() {}

    publicMethod() {}
}

assert(Test().getAttributes().len() == 3);
assert(Test().getAttributes().find(def(item) => item == "pub") == "pub");

class Inherit < Test {

}

assert(Inherit().getAttributes().len() == 3);
assert(Inherit().getAttributes().find(def(item) => item == "pub") == "pub");