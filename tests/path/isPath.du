/**
 * isPath.du
 *
 * Testing Path.isPath()
 *
 * Returns a boolean based on whether a string is syslink or not.
 */
import Path;
assert(Path.isPath("linktofile") == true);
assert(Path.isPath("notlinktofile") == false);