/**
 * isSysPath.du
 *
 * Testing Path.isSysPath()
 *
 * Returns a boolean based on whether a string is syslink or not.
 */
import Path;
assert(Path.isSysPath("linktofile") == true);
assert(Path.isSysPath("notlinktofile") == false);