/**
 * and.du
 *
 * Testing the and logical operator
 */

from UnitTest import UnitTest;

class AndOperatorTest < UnitTest {
    testAndOperator() {
        this.assertTruthy(true and true);
        this.assertFalsey(false and true);
        this.assertFalsey(true and false);
        this.assertFalsey(false and false);
    }
}

AndOperatorTest().run();